services:
  - type: web
    name: niramaya-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        value: https://healthcare-appointment-sigma.vercel.app
